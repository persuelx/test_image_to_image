1、gui操作界面第一个按钮selelct选取源图像(图像地址)
2、第二个按钮选择第二个图像(同上)
3、然后在两个文本输入框中分别输入0-1 代表目标图像的伸缩(对目标图像的宽高进行处理)，然后点击获取参数按钮。
4、在点击执行按钮即可。5、点击退出，退出整个界面。
6、按钮操作时尽量按顺序操作，(不按顺序可能会有bug)另外程序暂时不打包成exe了，环境里面的包太多，打包的话可能exe会很大。
7、时间原因程序难免会存在bug